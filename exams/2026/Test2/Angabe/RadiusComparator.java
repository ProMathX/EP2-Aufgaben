/**
 * A comparator that orders {@link Physical} objects
 * by their radius.
 *
 * <p>This comparator imposes an ordering based on the value
 * returned by {@link Physical#getRadius()}. In particular, a
 * positive return value of {@code compare(a, b)} means that {@code a}
 * has a larger radius than {@code b}. A negative return value means
 * that {@code a} has a smaller radius than {@code b}. In case of equal
 * radii the return value will be 0. </p>.
 *
 *  This class implements the {@link PhysicalComparator} interface.
 *
 */
public class RadiusComparator //implements PhysicalComparator //TODO: activate clause

{

    //TODO: define missing parts of this class.
    
}
